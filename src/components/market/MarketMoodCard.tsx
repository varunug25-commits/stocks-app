import { StyleSheet, Text, View } from "react-native";
import { colors, radii, spacing, typography } from "@/theme/tokens";
export function MarketMoodCard({ label, score, explanation }: { label: string; score: number; explanation: string }) { return <View accessibilityLabel={`Market mood ${label}. Score ${score} out of 100. ${explanation}`} style={styles.card}><View style={styles.top}><Text style={styles.eyebrow}>MARKET MOOD</Text><Text style={styles.score}>{score}/100</Text></View><Text style={styles.title}>{label}</Text><View style={styles.track}><View style={[styles.fill, { width: `${score}%` }]} /></View><Text style={styles.body}>{explanation}</Text></View>; }
const styles = StyleSheet.create({ card: { padding: spacing.lg, borderRadius: radii.hero, backgroundColor: colors.tealMuted, borderWidth: 1, borderColor: "#28584F" }, top: { flexDirection: "row", justifyContent: "space-between" }, eyebrow: { ...typography.caption, color: colors.teal, letterSpacing: 1 }, score: { ...typography.label, color: colors.teal }, title: { ...typography.heading, color: colors.textPrimary, marginTop: spacing.xs }, track: { height: 5, marginTop: spacing.md, borderRadius: radii.pill, backgroundColor: colors.surfaceSoft, overflow: "hidden" }, fill: { height: 5, backgroundColor: colors.teal }, body: { ...typography.body, color: colors.textSecondary, marginTop: spacing.md } });
