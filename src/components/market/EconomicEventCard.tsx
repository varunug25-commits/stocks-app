import { Ionicons } from "@expo/vector-icons";
import { StyleSheet, Text, View } from "react-native";
import type { EconomicEvent } from "@/data/markets";
import { colors, radii, spacing, typography } from "@/theme/tokens";
export function EconomicEventCard({ event }: { event: EconomicEvent }) { return <View accessibilityLabel={`${event.title}, ${event.day} at ${event.time}, ${event.impact} impact. ${event.context}`} style={styles.card}><View style={styles.icon}><Ionicons color={colors.warning} name="calendar-outline" size={20} /></View><View style={styles.copy}><View style={styles.top}><Text style={styles.title}>{event.title}</Text><Text style={styles.impact}>{event.impact}</Text></View><Text style={styles.detail}>{event.day} · {event.time}</Text><Text style={styles.context}>{event.context}</Text></View></View>; }
const styles = StyleSheet.create({ card: { flexDirection: "row", gap: spacing.sm, padding: spacing.md, borderRadius: radii.lg, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }, icon: { width: 40, height: 40, borderRadius: 14, alignItems: "center", justifyContent: "center", backgroundColor: "#302817" }, copy: { flex: 1 }, top: { flexDirection: "row", gap: spacing.xs }, title: { ...typography.label, flex: 1, color: colors.textPrimary }, impact: { ...typography.caption, color: colors.warning }, detail: { ...typography.caption, color: colors.textSecondary, marginTop: 3 }, context: { ...typography.caption, color: colors.textTertiary, marginTop: 3 } });
