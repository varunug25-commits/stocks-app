import { Pressable, StyleSheet, Text } from "react-native";
import { GlassBackdrop } from "@/components/foundation/GlassBackdrop";
import { colors, glass, radii, spacing, typography } from "@/theme/tokens";
export function FilterChip({ label, selected, onPress }: { label: string; selected: boolean; onPress: () => void }) { return <Pressable accessibilityRole="radio" accessibilityState={{ checked: selected }} onPress={onPress} style={[styles.chip, selected && styles.selected]}>{selected ? <GlassBackdrop intensity={16} /> : null}<Text style={[styles.text, selected && styles.selectedText]}>{label}</Text></Pressable>; }
const styles = StyleSheet.create({ chip: { minHeight: 44, justifyContent: "center", paddingHorizontal: spacing.md, borderRadius: radii.pill, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, overflow: "hidden" }, selected: { backgroundColor: glass.fallback, borderColor: glass.border }, text: { ...typography.label, color: colors.textSecondary }, selectedText: { color: colors.teal } });
